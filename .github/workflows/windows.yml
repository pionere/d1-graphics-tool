name: Windows

on: [ push, pull_request ]
#on:
#  push:
#    branches: [ "master" ]
#  pull_request:
#    branches: [ "master" ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v6

    - name: Install Qt
      uses: jurplel/install-qt-action@v4
      with:
        version: '6.8.3'
        cache: 'true'
        modules: 'qtmultimedia qtimageformats'

    # --config Release
    - name: Configure
      run: cmake -S. -Bbuild

    - name: Make
      run: cmake --build build -j $(nproc)

    #--no-virtualkeyboard
    - name: Package
      run: |
        mkdir dist
        copy build\Release\D1GraphicsTool.exe dist
        cd dist
        windeployqt D1GraphicsTool.exe --no-compiler-runtime --no-opengl-sw --no-system-d3d-compiler --no-system-dxc-compiler --no-translations --no-quick-import --no-ffmpeg
      shell: cmd

    - name: Upload
      if: ${{ !env.ACT }}
      uses: actions/upload-artifact@v7
      with:
        name: D1GraphicsTool-Windows-x64
        path: dist

    - name: Upload-translations
      #if: ${{ !env.ACT }}
      if: false
      uses: actions/upload-artifact@v7
      with:
        name: D1GraphicsTool-Translations
        path: languages

    - name: Upload bin
      if: ${{ !env.ACT }}
      uses: actions/upload-artifact@v7
      with:
        name: D1GraphicsTool.exe
        path: build\Release\D1GraphicsTool.exe
